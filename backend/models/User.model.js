// This is a placeholder file that doesn't use mongoose
// We're using the in-memory store instead

const User = {
  // Just placeholder methods to avoid errors if referenced
  findOne: () => null,
  findById: () => null,
  create: () => null
};

module.exports = User;
